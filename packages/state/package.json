{
  "name": "@commandui/state",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@commandui/domain": "workspace:*",
    "zustand": "^5.0.3"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"lint not configured yet\""
  },
  "devDependencies": {
    "vitest": "^3.0.8"
  }
}
