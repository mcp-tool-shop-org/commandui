[package]
name = "commandui-runtime-core"
version = "0.1.0"
edition = "2021"

[features]
test-utils = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
chrono = "0.4"
uuid = { version = "1", features = ["v4"] }
